# Personal Portfolio (Orazio Petito)

Minimal portfolio site.

## Local dev

```bash
npm install
npm run dev
```

## Deploy (Vercel)

- Import the repo into Vercel
- Framework: **Vite**
- Build: `npm run build`
- Output: `dist`

`vercel.json` includes an SPA rewrite so deep links work.
